# API

Prototype API surface is represented in the UI and domain docs.

Planned safe endpoints:
- profile;
- tiers;
- entitlement lookup;
- membership events/lists;
- post metadata;
- financial reconciliation summary without sensitive provider payloads.

API keys must be scoped, hashed, one-time displayed, rate limited, audited, and revocable.
