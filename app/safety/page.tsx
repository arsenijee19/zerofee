import { MarketingPage } from "@/components/route-ui";

export default function SafetyPage() {
  return <MarketingPage title="Money stays visible at every step." eyebrow="Safety and verification" />;
}
